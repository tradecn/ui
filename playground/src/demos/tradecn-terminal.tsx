import { ThemeSample } from "./shared/theme-sample"

// A theme installs no components. The page this renders on wears the theme; the sample is what it looks like.
export default function TradecnTerminalDemo() {
  return <ThemeSample />
}
